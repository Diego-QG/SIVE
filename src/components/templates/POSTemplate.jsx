import styled from "styled-components";

export function POSTemplate() {
    return (
    <Container>
        <Header>POS Template Component</Header>
    </Container>
    );
}
const Container = styled.div`
    height: 100vh;
    display: grid;
`

const Header = styled.div`
    
`